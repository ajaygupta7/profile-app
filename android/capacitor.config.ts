import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.profile.app',
  appName: 'profile-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
